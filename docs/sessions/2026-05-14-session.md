# RegressProof Session Note - 2026-05-14

## Completed

- Added root `action.yml` so `RegressProof-cli` can be used directly as a composite GitHub Action.
- Added README workflow example for `uses: tc7kxsszs5-cloud/RegressProof-cli@main`.
- Fixed GitHub output isolation during verification checks:
  - nested user commands no longer inherit `GITHUB_OUTPUT`
  - nested user commands no longer inherit `GITHUB_STEP_SUMMARY`
  - nested user commands no longer inherit `GITHUB_ENV`
  - nested user commands no longer inherit `GITHUB_PATH`
- Documented explicit `--out-dir` usage for low-space temp environments.

## Verification

- `npm run build`: passed
- `npm run external:check`: passed
- `npm run validate:faults`: passed
- committed validation smoke with `GITHUB_OUTPUT`: passed
- `npm run verify:mvp -- --out-dir /private/tmp/regressproof-verify-action-review`: passed

## Notes

- The earlier default `npm run verify` failure was caused by `ENOSPC` in the system temp directory, not by a RegressProof logic failure.
- Running the same MVP verification with an explicit output directory completed successfully:
  - fixtures: `11/11`
  - trust scenario: `successful_change / high`
  - deep scenario: `successful_change / high`
