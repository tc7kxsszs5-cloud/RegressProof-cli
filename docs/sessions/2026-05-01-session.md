# RegressProof Session Note

**Date:** 2026-05-01
**Focus:** Promote a fresh pinned external corpus run from the canonical `RegressProof-cli` repository
**Status:** completed

## Completed

- Ran a fresh pinned external validation against `pmndrs/zustand`.
- Confirmed the committed range `6213fc11bdf096301a82ae5c236b5a666a4ee3ca~1..6213fc11bdf096301a82ae5c236b5a666a4ee3ca`.
- Confirmed changed files:
  - `src/middleware/persist.ts`
  - `tests/persistAsync.test.tsx`
- Confirmed result: `successful_change / high`.
- Recorded the run in `examples/external-runs.json`.
- Updated README proof counts and most-recent-run snapshot.
- Added a new case study entry for `pmndrs/zustand`.
- Updated index and workflow memory to reflect the new canonical corpus size.

## Changed Files

- `README.md`
- `docs/REGRESSPROOF_CASE_STUDIES.md`
- `docs/REGRESSPROOF_INDEX.md`
- `docs/REGRESSPROOF_WORKFLOW_MEMORY.md`
- `docs/sessions/2026-05-01-session.md`
- `examples/external-runs.json`

## Decisions

- Treat the old short SHA from workspace memory as historical guidance only until revalidated from the canonical standalone repository.
- Prefer promoting externally prepared configs only after a fresh standalone-side run produces a preserved artifact and a complete pinned run record.

## Open Questions

- `pydantic/pydantic` is still described in memory and supported modes, but it is not yet present in the canonical `external-runs.json` catalog.
- `npm run verify` remains heavier than the narrower trust commands and should be rechecked again after the standalone trust-default fixes settle.

## Next Best Step

- Revalidate `pydantic/pydantic` from the canonical standalone repository and promote it into `examples/external-runs.json` if the fresh pinned run still succeeds.
