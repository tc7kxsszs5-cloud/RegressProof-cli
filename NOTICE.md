# RegressProof Notice

`RegressProof` is a proof-based regression detection and accountability system
for AI coding changes.

Core public identity:

- product name: `RegressProof`
- tagline: `Proof, not guesses, for agent-caused regressions.`
- category: CLI and GitHub Action utility for AI coding regression detection
- owner: `tc7kxsszs5-cloud`

The repository contains original source code, product documentation, validation
plans, decision logs, example configurations, and curated external validation
evidence for RegressProof.

The curated validation corpus currently includes pinned public-repository runs
for repositories such as:

- `openclaw/openclaw`
- `sindresorhus/ky`
- `unjs/ofetch`
- `nanostores/nanostores`

The validation records, artifact references, examples, and case-study structure
are part of the RegressProof project materials and should not be reused to imply
authorization, endorsement, or ownership by another project.

No trademark rights are granted by this notice. The RegressProof name and
tagline are reserved by the project owner. This notice is not legal advice.
